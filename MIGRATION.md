# Миграция на Production-уровень

## ✅ Выполненные изменения

### 1. SQLite БД вместо JSON
- Создан `database.py` с таблицами: `trades`, `orders`, `pnl_snapshots`, `events`
- Все операции записи состояния идут через БД
- JSON файлы (`trades_log.json`, `pnl_history.json`) больше не используются как источник истины
- Экспорт в JSON доступен через `/trades_export` для обратной совместимости

### 2. Чёткое разделение ответственности

**trader.py**
- Единственный модуль, который отправляет ордера в Binance
- Функции: `place_trade_with_tp_sl()`, `close_trade_manually()`, `update_tp_sl()`
- Все операции логируются в БД

**monitor.py**
- НЕ принимает торговых решений
- НЕ закрывает сделки по цене
- Только проверяет статусы ордеров на Binance
- Синхронизирует БД с реальным состоянием
- Обновляет статусы сделок при исполнении TP/SL

**risk.py**
- Проверка риска перед каждой сделкой
- Лимиты: риск на сделку ≤ 0.5%, дневной убыток < 2%, drawdown < 10%
- Проверка волатильности и количества позиций

### 3. Режимы торговли

- **CONFIRM_ALL** - все сделки требуют подтверждения
- **AUTO_GATED** (по умолчанию) - автосделки с проверкой риска
- **AUTO_ALL** - полностью автоматические (выключено)

Управление через `.env`:
```
TRADING_MODE=AUTO_GATED
```

Или через API:
```
GET /trading_mode
POST /trading_mode {"mode": "AUTO_GATED"}
```

### 4. Безопасный Monitor

Monitor теперь:
- Проверяет статусы ордеров на Binance
- Обновляет БД при исполнении TP/SL
- Логирует расхождения
- НЕ выставляет ордера
- НЕ закрывает сделки

### 5. Обновлённый GUI

Добавлено:
- Индикатор режима торговли
- Закрытие позиций вручную
- Редактирование TP/SL
- Обновлённый график PnL из БД
- Работа с новыми API эндпоинтами

## 🚀 Запуск системы

### 1. Инициализация БД
БД автоматически создаётся при первом импорте `database.py`.

### 2. Запуск сервера
```bash
cd binance
python app.py
```

### 3. Запуск мониторинга (в отдельном процессе)
```bash
python monitor.py
```

### 4. Запуск скальпера (опционально, в отдельном процессе)
```bash
python scalper.py
```

### 5. Запуск GUI
```bash
cd ..
python gui.py
```

## 📋 Новые API эндпоинты

- `POST /close_trade` - закрытие позиции вручную
- `POST /update_tp_sl` - обновление TP/SL
- `GET /trading_mode` - получение режима торговли
- `POST /trading_mode` - установка режима торговли
- `GET /risk_summary` - сводка по рискам
- `GET /all_trades` - все сделки из БД
- `GET /trades_export` - экспорт в JSON формат

## ⚠️ Важные замечания

1. **Миграция данных**: Старые JSON файлы не мигрируются автоматически. Если нужно сохранить историю, создайте скрипт миграции.

2. **Режим торговли**: По умолчанию установлен `AUTO_GATED` - безопасный режим с проверкой рисков.

3. **БД файл**: `trades.db` создаётся автоматически в папке `binance/`. Рекомендуется делать резервные копии.

4. **Логирование**: Все события логируются в таблицу `events` в БД.

5. **Безопасность**: Monitor не может случайно закрыть позиции - он только проверяет статусы.

## 🔧 Настройка

Все настройки в `.env`:
```
BINANCE_API_KEY=xxx
BINANCE_API_SECRET=xxx
TELEGRAM_TOKEN=xxx
TELEGRAM_ID=xxx
ENABLE_TELEGRAM_ALERTS=True
TRADING_MODE=AUTO_GATED
```

## 📊 Мониторинг

Проверка состояния:
- Логи в БД: таблица `events`
- Открытые позиции: `GET /open_trades`
- Риски: `GET /risk_summary`
- PnL история: `GET /pnl_history`

